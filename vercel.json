{
  "version": 2,
  "builds": [
    {
      "src": "Cargo.toml",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "cargo build --release --target x86_64-unknown-linux-musl"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}